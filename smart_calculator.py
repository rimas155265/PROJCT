def smart_calculator():
    while True:
        print("\nمرحبًا بك في الآلة الحاسبة الذكية 🧠")
        
        # 1. إدخال الرقمين
        try:
            num1 = float(input("أدخل الرقم الأول: "))
            num2 = float(input("أدخل الرقم الثاني: "))
        except ValueError:
            print("يرجى إدخال أرقام صحيحة.")
            continue

        # 2. اختيار العملية
        print("\nاختر العملية التي تريد تنفيذها:")
        print("1. الجمع (+)")
        print("2. الطرح (-)")
        print("3. الضرب (*)")
        print("4. القسمة (/)")
        print("5. الأس (**)")
        operation = input("اكتب الرمز المناسب للعملية: ")

        # 3. تنفيذ العملية باستخدام if / elif / else
        if operation == '+':
            result = num1 + num2
            print(f"الناتج: {num1} + {num2} = {result}")
        elif operation == '-':
            result = num1 - num2
            print(f"الناتج: {num1} - {num2} = {result}")
        elif operation == '*':
            result = num1 * num2
            print(f"الناتج: {num1} * {num2} = {result}")
        elif operation == '/':
            if num2 != 0:
                result = num1 / num2
                print(f"الناتج: {num1} / {num2} = {result}")
            else:
                print("خطأ: لا يمكن القسمة على صفر.")
                continue
        elif operation == '**':
            result = num1 ** num2
            print(f"الناتج: {num1} ** {num2} = {result}")
        else:
            print("عملية غير معروفة. يرجى اختيار رمز صحيح.")
            continue

        # 4. مقارنة الرقمين
        if num1 > num2:
            print("الرقم الأول أكبر من الرقم الثاني.")
        elif num1 < num2:
            print("الرقم الأول أصغر من الرقم الثاني.")
        else:
            print("الرقمان متساويان.")

        # شرط إضافي: موجبة أو سالبة
        print("\nتحليل الإشارات:")
        print(f"الرقم الأول {'موجب' if num1 > 0 else 'سالب' if num1 < 0 else 'صفر'}")
        print(f"الرقم الثاني {'موجب' if num2 > 0 else 'سالب' if num2 < 0 else 'صفر'}")

        # خيار إعادة العملية
        again = input("\nهل ترغب في إجراء عملية أخرى؟ (نعم/لا): ").strip().lower()
        if again != 'نعم':
            print("شكرًا لاستخدام الآلة الحاسبة الذكية! 👋")
            break

# تشغيل البرنامج
smart_calculator()